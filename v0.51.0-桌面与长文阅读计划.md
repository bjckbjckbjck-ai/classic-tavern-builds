# v0.51 桌面与长文阅读实施计划

> 实施流程：subagent-driven-development，逐项实现、规格复核和质量复核；使用当前获准3d分支。此计划延续用户确认的下一阶段，不增加暂停、种族或平衡改动。

**目标：** 弱化重复木纹，形成安静桌心和较暗边缘；让规则、公告与完整技能能在手机上清楚阅读。

**结构：** 继续共用 `tavern_surface.gdshaderinc` 保持2D/3D一致，复用当前真实3D桌板和卡牌投影。阅读页委托小型共用控件，`library_ui.gd` 保留内容与返回路由。

**技术：** Godot 4.6.1、GDScript、Compatibility shader、一次2×2手绘材质图集。

## 方案取舍与实际参考

已再次查看玩家6c984d0c手机实图与本作mobile-ten实截图：原版桌心低对比，较粗边缘形成深浅区；本作横向拼板线过重、上下区域像均匀面板。公告截图正文贴边、深底白字密集。采取材质和共用渐变调整，保留原布局及已有边框；全景重画会带来更大裁切风险，纯粹提亮原图则无法消除木纹重复。

四格素材安排：左上 `table-center` 蜂蜜棕低对比木面，少量宽松不规则木纹；右上 `table-edge` 深胡桃木；左下 `reading-paper` 浅暖纸页；右下 `reading-leather` 深棕皮革。无文字、徽章、角色、卡框。按格裁切，保留原图、提示词、映射和哈希；阅读小图与桌面实际检查后再使用，不把备用素材写为已使用。

## 任务

- [x] 补齐上轮独立质量审查：已完成代码检查及两个有界headless复跑，未发现需修复问题。原生渲染仍引用已保存实际检查，不冒充重新真机验收。
- [x] 内置image_gen生成1254×1254四格图集（实际输出尺寸），各格1px安全内缩后裁成625×625，保存原图、提示词、脚本、坐标与哈希；冻结四个材质后接入。
- [x] 桌面：共用shader与两处绑定已完成；独立规格和质量审查通过，补齐零向量归一化保护。原生28条检查通过，含2D/3D三点颜色一致性；未新增实时灯、粒子、视口或修改操作坐标。实际查看16:9/20:9、2D/3D四张截图。
- [x] 阅读页：共用居中书页控件，标题/返回独立，正文缩窄并留出足够纸页边距，竖向滚动可触摸/滚轮/键盘。公告、规则、技能保留完整文字和返回路由；不解析不可信富文本、不暂停回合、不新增外链操作。
- [x] 各项独立规格/质量复核；有意义的原生回归与实际截图，保留失败与修正记录，不做平衡模拟。
- [ ] 更新仓库与游戏内开发公告、下一步、证据；本地提交源代码、公开仓库仅提交文档与截图，回复#18保持其余范围开放。

## 文件与验证

- 素材：`assets/generated/v51-ui/`；原图及处理脚本、prompt、mapping单独保存。
- 桌面：`scripts/tavern_surface.gdshaderinc`、`scripts/main.gd`背景绑定、`scripts/presentation3d/tavern_stage.gd`桌面绑定。复用`layout_v46_test`、`hand_platform_v47_test`及实际截图；测量绘制是否引入额外视口/全帧重建，不据此声称实体手机加速。
- 阅读：新`scripts/reading_page.gd`，修改`library_ui.gd:scrolling_text`；目标测试覆盖长文末尾、真实滚轮/触摸、返回、宽屏、招募计时继续、无横向溢出，常规入口回归。
- 正式更新器/项目/导出版本保持0.50.0；本轮不宣称新APK已经验收。后续统一v0.51发行时再覆盖安装与联机最终包验证。

补充结算路由边界修复：计时、2D动画和3D延迟收尾统一经过render；修复前3项失败，修复后阅读31条通过。完整证据见[本阶段记录](validation/issue18-table-reading/README.md)。
