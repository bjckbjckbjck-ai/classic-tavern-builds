# 项目协作约定

本文件适用于当前 Godot 项目及其子目录。

## 美术风格：接近炉石原版的卡牌手绘感

- 用户认为 v0.39.0 的新插画过于精致。后续新增或重绘素材，优先参考炉石原版的卡牌插画风格：夸张造型、鲜明轮廓、饱满色块、可见笔触和幽默神态。
- 画面重心放在角色、道具与效果辨识上。简化背景和细碎纹理，使用明确的冷暖对比与明暗分组，使小尺寸卡牌也能一眼认出主体。
- 避免写实照片感、电影级光照、密集金属雕花、过量粒子和繁复背景；不要以增加细节或写实程度作为美化的默认方向。
- 原创卡可以沿用这种整体美术语言，但应围绕自身效果设计独立形象，不直接借用另一张已有卡图代替。

## 素材生产：优先批量图集，再裁剪

- 用户明确偏好一次生成包含多张素材的图集，再裁剪为独立文件；不再默认每张卡各调用一次生图。
- 按种族、流派、系列或用途分批，通常每批 4–6 张，采用规则的 2×2 或 3×2 网格。优先保证每格的清晰度和风格一致性，复杂批次可减少张数。
- 生图前确定每格对应的卡牌 ID、主体、动作、配色和裁剪用途，在提示词中明确每格的位置与内容，避免重复角色、串格和跨格特效。
- 每格使用一致尺寸，主体完整、居中并留出安全边距；不用文字、卡名、数值、卡框或水印作为图像内容。分隔留白应方便裁剪，不能侵入最终素材。
- 为不同用途预留构图：随从椭圆窗口需保住脸与关键道具，手牌和图鉴的横向窗口需保住视觉重点。禁止靠拉伸人物填满窗口。
- 按网格坐标裁剪，再按需缩放、压缩和导入 Godot；以卡牌 ID 命名独立资源。保留原始图集、格位映射、提示词和处理说明，便于后续补图与重做。
- 验收既检查整张图集，也检查裁剪后的独立图；必须放进游戏的实际小尺寸卡面、图鉴和手机界面检查。确认无串格、切头、关键道具丢失或比例变形后再交付。
- 某一格不合格时优先局部重做或另批补图；只有角色构图确实需要或用户另有要求时，才单独生成一张。

本约定用于后续素材制作；仅更新本文件不意味着自动重绘或替换已经发布的素材。
