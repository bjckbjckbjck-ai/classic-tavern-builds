diff --git a/export_presets.cfg b/export_presets.cfg
index a19d1a2..d8f3c13 100644
--- a/export_presets.cfg
+++ b/export_presets.cfg
@@ -43,10 +43,10 @@ architectures/armeabi-v7a=true
 architectures/arm64-v8a=true
 architectures/x86=false
 architectures/x86_64=true
-version/code=65
-version/name="0.61.0"
-package/unique_name="org.classictavern.game"
-package/name="经典酒馆"
+version/code=7
+version/name="0.61.0-service7"
+package/unique_name="org.classictavern.service"
+package/name="云端酒馆"
 package/signed=true
 launcher_icons/main_192x192="res://assets/icon.svg"
 screen/immersive_mode=true
diff --git a/scripts/foyer_ui.gd b/scripts/foyer_ui.gd
index 499a72d..22e0414 100644
--- a/scripts/foyer_ui.gd
+++ b/scripts/foyer_ui.gd
@@ -7,7 +7,9 @@ static func menu(app):
 	app.label("七个种族 · 全明星叠层",Rect2(234,540,340,38),22,"e9c98e",true)
 	app.label("招募随从，成为最后的赢家",Rect2(234,580,340,32),17,"d8bf96",true)
 	ART.button(app,"进入酒馆",Rect2(704,270,435,90),app.hero_draft,false,true)
-	ART.button(app,"好友联机",Rect2(724,378,395,78),app.network_menu)
+	ART.button(app,"好友房间",Rect2(704,378,215,78),func():app.cloud.open("friend"))
+	ART.button(app,"在线匹配",Rect2(935,378,215,78),func():app.cloud.open("ranked"))
+	app.button("本地局域网",Rect2(244,605,375,40),app.network_menu)
 	ART.button(app,"卡池与英雄图鉴",Rect2(724,475,395,70),func():app.COLLECTION.open_kind(app,"cards"))
 	ART.button(app,"畸变："+app.RULES.ANOMALY.label(app.anomaly_mode),Rect2(724,558,395,62),func():preload("res://scripts/anomaly_ui.gd").settings(app))
 	ART.button(app,"BOT："+app.RULES.AI.NAMES[app.ai_difficulty],Rect2(244,661,375,58),app.difficulty_menu)
@@ -15,7 +17,7 @@ static func menu(app):
 	ART.button(app,"设置",Rect2(196,787,190,50),func():app.SETTINGS.show(app))
 	ART.button(app,"流派与规则",Rect2(405,787,235,50),func():app.LIBRARY.strategies(app))
 	ART.button(app,"版本公告",Rect2(660,787,235,50),func():app.LIBRARY.journal(app))
-	ART.button(app,"自动更新",Rect2(916,787,300,50),func():app.LIBRARY.updates(app))
+	ART.button(app,"发现新版 · 一键更新" if not app.updater.download_url.is_empty() else "客户端更新",Rect2(916,787,300,50),func():app.LIBRARY.updates(app)).name="ClientUpdateButton"
 	app.label(app.lobby_error,Rect2(245,725,940,32),16,"ffb18f",true);app.footer();ART.enter(app)
 static func network(app):
 	app.clear();ART.shade(app);ART.art(app,"collection_book",Rect2(160,74,1120,728))
@@ -43,6 +45,7 @@ static func lobby(app):
 	for ip in IP.get_local_addresses():
 		if "." in ip and not ip.begins_with("127.") and not ip.begins_with("169.254."):ips.append(ip)
 	var address="房主地址："+" / ".join(ips)+"  · 端口 %d"%app.port if app.multiplayer.is_server() else ("连接成功 · 你是管理员" if app.is_manager() else "连接成功 · 等待管理员开始")
+	if app.cloud and app.cloud.active:address="云服房间码："+app.cloud.room_code+" · 断线期间AI托管"
 	app.label(address,Rect2(150,134,1150,37),17,"dbc496")
 	for i in range(8):
 		var pos=Vector2(160+(i%4)*195,205+int(i/4)*230);var p=app.state.players[i] if i<app.state.players.size() else {}
@@ -56,13 +59,18 @@ static func lobby(app):
 		else:role=""
 		app.label(role,Rect2(pos+Vector2(-10,196),Vector2(178,24)),13,"c3b59e",true)
 		if p.get("bot",false) and app.is_manager():ART.button(app,"×",Rect2(pos+Vector2(130,14),Vector2(40,40)),func():app.room_command("remove",int(p.id)))
-	app.label("选择英雄",Rect2(997,192,284,42),25,"ffe7b1",true)
-	for i in range(app.state.me.hero_offers.size()):
-		var index=int(app.state.me.hero_offers[i]);var h=app.catalog.heroes[index]
-		var b=ART.button(app,("✓ " if int(app.state.me.hero)==index else "")+h.name,Rect2(986,246+i*70,298,56),func():app.selected_hero=index;app.request("hero",index),false,int(app.state.me.hero)==index);b.set_meta("lobby_hero",index)
-	var hero=app.catalog.heroes[int(app.state.me.hero)]
-	app.label("生命 %d · 护甲 %d"%[int(hero.get("hp",40)),int(hero.get("armor",0))],Rect2(994,537,280,32),18,"e7c787",true)
-	var description=RichTextLabel.new();description.text=hero.text;description.position=Vector2(995,580);description.size=Vector2(290,122);description.add_theme_font_size_override("normal_font_size",18);description.scroll_active=true;app.ui.add_child(description)
+	if app.cloud and app.cloud.active:
+		app.label("等待房主开始",Rect2(997,230,284,60),26,"ffe7b1",true)
+		app.label("开始后进入四选一英雄界面",Rect2(972,310,335,90),20,"dbc496",true)
+	else:
+		app.label("选择英雄",Rect2(997,192,284,42),25,"ffe7b1",true)
+		for i in range(app.state.me.hero_offers.size()):
+			var index=int(app.state.me.hero_offers[i]);var h=app.catalog.heroes[index]
+			var b=ART.button(app,("✓ " if int(app.state.me.hero)==index else "")+h.name,Rect2(986,246+i*70,298,56),func():app.selected_hero=index;app.request("hero",index),false,int(app.state.me.hero)==index);b.set_meta("lobby_hero",index)
+		var hero=app.catalog.heroes[int(app.state.me.hero)]
+		app.label("生命 %d · 护甲 %d"%[int(hero.get("hp",40)),int(hero.get("armor",0))],Rect2(994,537,280,32),18,"e7c787",true)
+		var description=RichTextLabel.new();description.text=hero.text;description.position=Vector2(995,580);description.size=Vector2(290,122);description.add_theme_font_size_override("normal_font_size",18);description.scroll_active=true;app.ui.add_child(description)
+
 	app.label("%d / 8 位客人"%app.state.players.size(),Rect2(164,685,220,34),21,"ffe7ad")
 	ART.button(app,"饰品："+("开启" if app.state.get("trinkets_enabled",false) else "关闭"),Rect2(423,683,225,48),func():app.room_command("trinkets"),not app.is_manager())
 	ART.button(app,"BOT："+app.RULES.AI.NAMES[int(app.state.ai_difficulty)],Rect2(674,683,253,48),func():app.room_command("difficulty"),not app.is_manager())
diff --git a/scripts/hero_draft_ui.gd b/scripts/hero_draft_ui.gd
index ab21da6..cbe0612 100644
--- a/scripts/hero_draft_ui.gd
+++ b/scripts/hero_draft_ui.gd
@@ -40,11 +40,23 @@ func _ready():
 	# Buttons use the common visual language, but stay children of this centered composition.
 	back_button=make_button("返回",Rect2(166,758,170,58),app.menu,false);back_button.name="Back"
 	confirm_button=make_button("准备好了 · 进入酒馆",Rect2(470,750,500,68),app.practice_room,true);confirm_button.name="Confirm"
+	if app.cloud and app.cloud.active and app.state.get("phase","")=="hero_select":
+		for callback in confirm_button.pressed.get_connections():confirm_button.pressed.disconnect(callback.callable)
+		confirm_button.pressed.connect(app.cloud.confirm_hero,CONNECT_DEFERRED)
+		for callback in back_button.pressed.get_connections():back_button.pressed.disconnect(callback.callable)
+		back_button.pressed.connect(app.leave_room,CONNECT_DEFERRED)
+		back_button.text="断开 / 托管"
+		if app.state.get("hero_confirmed",false):
+			confirm_button.text="已确认 · 等待其他玩家";confirm_button.disabled=true
+		var timer=caption("",Rect2(1020,100,240,38),20,"ffe4a6")
+		app.timer_label=timer
+		caption("30秒内确认 · 超时自动选择",Rect2(280,180,880,28),17,"d5bf9a")
 	caption("点击肖像选择 · 下方查看技能",Rect2(1010,765,292,40),16,"cdbb9b")
 	_update_skill()
 
 func caption(text:String,rect:Rect2,font_size:int,color:String):
 	var label=Label.new();label.text=text;label.position=rect.position;label.size=rect.size;label.horizontal_alignment=HORIZONTAL_ALIGNMENT_CENTER;label.vertical_alignment=VERTICAL_ALIGNMENT_CENTER;label.mouse_filter=Control.MOUSE_FILTER_IGNORE;label.add_theme_font_size_override("font_size",font_size);label.add_theme_color_override("font_color",Color(color));label.add_theme_color_override("font_shadow_color",Color("160e0d"));label.add_theme_constant_override("shadow_offset_y",2);add_child(label)
+	return label
 
 func make_button(text:String,rect:Rect2,callback:Callable,primary:bool)->Button:
 	var button=Button.new();button.text=text;button.position=rect.position;button.size=rect.size
@@ -54,6 +66,7 @@ func make_button(text:String,rect:Rect2,callback:Callable,primary:bool)->Button:
 	return button
 
 func _select(index:int):
+	if app.cloud and app.cloud.active and app.state.get("hero_confirmed",false):return
 	if index not in app.draft_offers or index==app.selected_hero:return
 	app.selected_hero=index;app.feedback.cue("hero_select")
 	for card in cards:card.set_selected(card.hero_index==index)
diff --git a/scripts/library_ui.gd b/scripts/library_ui.gd
index 2ef4e40..276f6a2 100644
--- a/scripts/library_ui.gd
+++ b/scripts/library_ui.gd
@@ -31,13 +31,14 @@ static func journal(app,page:int=0):
 static func updates(app):
 	app.menu_page="updates";app.clear()
 	app.panel(Rect2(260,170,920,580),"282021f5")
-	app.label("自动更新 · 手动检查",Rect2(320,200,800,65),34,"ffe4a6",true)
+	app.label("云服客户端更新",Rect2(320,200,800,65),34,"ffe4a6",true)
 	app.label("当前版本 v"+app.updater.VERSION,Rect2(320,278,800,40),22,"cdb990",true)
 	var info=app.label(app.updater.message,Rect2(340,345,760,160),22,"e2cba3",true)
+	if not app.state.is_empty():info.text+="\n请先结束对局再安装更新。"
 	info.autowrap_mode=TextServer.AUTOWRAP_ARBITRARY;info.text_overrun_behavior=TextServer.OVERRUN_NO_TRIMMING
 	app.button("检查新版本",Rect2(325,550,235,60),app.updater.check,app.updater.busy,true)
-	app.button("下载新版本",Rect2(605,550,235,60),func():OS.shell_open(app.updater.download_url),app.updater.download_url.is_empty())
-	app.button("发布页",Rect2(885,550,235,60),func():OS.shell_open(app.updater.REPO+"/releases/latest"))
+	app.button("安装并重启" if app.updater.ready_to_install() else "一键更新",Rect2(605,550,235,60),func():app.updater.restart_install() if app.updater.ready_to_install() else app.updater.install(),app.updater.download_url.is_empty() or app.updater.busy or not app.state.is_empty())
+	app.button("手动下载",Rect2(885,550,235,60),func():OS.shell_open(app.updater.download_url if not app.updater.download_url.is_empty() else app.updater.REPO))
 	app.button("返回酒馆",Rect2(510,656,420,55),func():app.menu_page="";app.menu())
 static func heroes(app):
 	app.gallery_kind="heroes";app.COLLECTION.show(app)
diff --git a/scripts/main.gd b/scripts/main.gd
index c5a7778..69bc641 100644
--- a/scripts/main.gd
+++ b/scripts/main.gd
@@ -114,10 +114,12 @@ const FOYER=preload("res://scripts/foyer_ui.gd")
 var hero_page=0
 var draft_offers:Array=[]
 var music
+var cloud
 var hand_controls
 const NETWORK_VERSION = "allstars-0.61.0-ws"
 
 func _ready():
+	cloud=preload("res://scripts/service_client.gd").new();cloud.app=self;add_child(cloud)
 	layout.setup(self)
 	hand_controls=preload("res://scripts/hand_controls.gd").new();hand_controls.app=self;add_child(hand_controls)
 	var video_config=ConfigFile.new();video_config.load("user://preferences.cfg")
@@ -186,9 +188,10 @@ func _ready():
 	multiplayer.connected_to_server.connect(func(): register_player.rpc_id(1,nickname,NETWORK_VERSION))
 	multiplayer.connection_failed.connect(func(): leave_room("连接失败，请检查服务器地址和网络"))
 	multiplayer.server_disconnected.connect(func(): leave_room("房主已断开，房间关闭"))
-	updater=preload("res://scripts/update_checker.gd").new();add_child(updater)
+	updater=preload("res://scripts/service_updater.gd").new();add_child(updater)
 	updater.completed.connect(func():
-		if menu_page=="updates":LIBRARY.updates(self))
+		if menu_page=="updates":LIBRARY.updates(self)
+		elif ui.has_node("ClientUpdateButton") and not updater.download_url.is_empty():ui.get_node("ClientUpdateButton").text="发现新版 · 一键更新")
 	var arrow=preload("res://scripts/target_arrow.gd").new();arrow.app=self;add_child(arrow)
 	var purchase_catcher=preload("res://scripts/purchase_catcher.gd").new();purchase_catcher.app=self;add_child(purchase_catcher)
 	menu()
@@ -502,7 +505,9 @@ func content(cards: Dictionary,heroes: Array,spells:Array,trinkets:Array,prizes:
 @rpc("authority","call_remote","reliable")
 func rejected(reason: String): leave_room(reason)
 
-func leave_room(reason: String = ""):
+func leave_room(reason: String = "",local_only:bool=false):
+	if cloud and cloud.active and reason.is_empty() and not local_only:cloud.leave_options();return
+	if cloud and cloud.active:cloud.stop()
 	action_history.clear()
 	online=false
 	for child in get_children():
@@ -574,7 +579,8 @@ func request(action: String,index: int = -1,target: int = -1):
 	for card in cards_ui:
 		if card.zone==origin_zone and card.index==index:pending_origin=card.position+card.size/2
 	var guard=RULES.action_guard(state.get("me",{}),action,index,target)
-	if multiplayer.is_server(): handle(1,action,index,target,guard)
+	if cloud and cloud.active:cloud.intent(action,index,target,guard)
+	elif multiplayer.is_server(): handle(1,action,index,target,guard)
 	else: intent.rpc_id(1,action,index,target,guard)
 
 @rpc("any_peer","call_remote","reliable")
@@ -642,7 +648,7 @@ func receive_state(value: Dictionary):
 	# Other seats changing their shops must not destroy an in-progress local drag.
 	if value.get("phase","")=="recruit" and state.get("phase","")=="recruit" and value.get("round",0)==state.get("round",0) and value.get("me",{})==state.get("me",{}) and value.get("watch_id",0)==state.get("watch_id",0):
 		state=value
-		if not multiplayer.is_server():deadline=now()+float(value.get("remaining",60))
+		if (cloud and cloud.active) or not multiplayer.is_server():deadline=now()+float(value.get("remaining",60))
 		return
 	if is_instance_valid(battlecry_preview):battlecry_preview.close()
 	var old=state.duplicate(true)
@@ -657,7 +663,7 @@ func receive_state(value: Dictionary):
 	if old.get("watch_id",0)!=state.get("watch_id",0) and state.get("spectating",false):feedback.cue("spectate")
 	elif int(old.get("round",0))!=int(state.get("round",0)) and state.get("phase","")=="recruit":feedback.cue("opponent")
 	if old.is_empty() or state.phase=="lobby":recruit_seen=0
-	if not multiplayer.is_server():deadline=now()+float(value.get("remaining",60))
+	if (cloud and cloud.active) or not multiplayer.is_server():deadline=now()+float(value.get("remaining",60))
 	if state.phase=="combat" and (replay_round!=int(state.round) or old.get("watch_id",0)!=state.get("watch_id",0)):
 		replay_round=int(state.round)
 		replay_index=0
@@ -710,6 +716,7 @@ func receive_state(value: Dictionary):
 var counter_state:Dictionary={}
 var counter_scope=""
 func render():
+	if menu_page=="cloud_leave":return
 	if menu_page=="anomalies":
 		if state.is_empty() or state.get("phase","")=="lobby":
 			load("res://scripts/anomaly_ui.gd").settings(self,not state.is_empty());return
@@ -727,6 +734,8 @@ func render():
 		if not ui.has_node("CollectionContent"):show_gallery()
 		return
 	if state.is_empty():menu();return
+	if cloud and cloud.active and cloud.spectator and state.phase in ["lobby","hero_select"]:cloud.spectator_lobby(state);return
+	if cloud and cloud.active and state.phase=="hero_select":cloud.hero_draft();return
 	if state.phase=="lobby":anomaly_intro_seen="";lobby();return
 	table()
 
@@ -876,7 +885,13 @@ func table():
 			hero_image(catalog.heroes[int(results[i].hero)],Rect2(463,y,40,43),results[i].hp<=0)
 			label(results[i].name,Rect2(521,y,260,43),21,"eed8b1")
 			label(catalog.heroes[int(results[i].hero)].name,Rect2(786,y,370,43),20,"eed8b1")
-		button("再来一局" if is_manager() else "等待管理员重新开局",Rect2(568,723,310,64),func():room_command("rematch"),not is_manager(),true)
+		if cloud and cloud.active:
+			button("返回云端大厅",Rect2(568,723,310,64),func():
+				var mode=cloud.desired
+				leave_room("",true)
+				cloud.open(mode),false,true)
+		else:
+			button("再来一局" if is_manager() else "等待管理员重新开局",Rect2(568,723,310,64),func():room_command("rematch"),not is_manager(),true)
 	UX.decorate(self,p)
 	var h:Dictionary=catalog.heroes[int(p.hero)]
 	var hero_rect=layout.hero_rect();var hero_y=hero_rect.position.y
@@ -909,6 +924,7 @@ func table():
 	if recruiting and not recruit_lock_reason().is_empty():toast_label.text=recruit_lock_reason();toast_label.add_theme_color_override("font_color",Color("ffd075"))
 	if settling:toast_label.text="回合已结束 · 正在展示回合末效果，随后自动进入战斗"
 	if state.get("viewer_dead",false):toast_label.text=("观战："+p.name+" · 点击左侧存活玩家切换" if state.get("spectating",false) else "已淘汰 · 点击左侧存活玩家观战");toast_label.modulate=Color("98f7ea")
+	if cloud and cloud.active and cloud.spectator:toast_label.text="公开观战："+p.name+" · 手牌与商店隐藏"
 	inspector=label("",Rect2(270,344 if layout.desktop_hud() else 622,650,28),14,"e4d1b0",true)
 	TRINKET_UI.owned(self)
 	if state.phase=="finished":timer_label.hide()
@@ -945,6 +961,7 @@ func combat_public(player:Dictionary)->Dictionary:
 
 func skip_battle():
 	if state.get("phase","")!="combat" or state.me.hp<=0:return
+	if cloud and cloud.active:cloud.finish_replay();return
 	if multiplayer.multiplayer_peer is OfflineMultiplayerPeer:
 		if game.phase!="combat" or game.player(1).hp<=0:return
 		game.finish_combat();deadline=now()+recruit_seconds();broadcast()
@@ -1231,7 +1248,7 @@ func _process(delta):
 
 	frame_samples.append(delta*1000.0)
 	if frame_samples.size()>600:frame_samples.pop_front()
-	var own_penalty=game.TOMORROW.penalty(game,game.player(1)) if multiplayer.is_server() and game!=null and game.phase=="recruit" else 0.0
+	var own_penalty=game.TOMORROW.penalty(game,game.player(1)) if (multiplayer.is_server() and not (cloud and cloud.active)) and game!=null and game.phase=="recruit" else 0.0
 	feedback.update_timer(state.get("phase",""),int(state.get("round",0)),deadline-now()-own_penalty,not state.get("viewer_dead",false) and state.get("me",{}).get("hp",0)>0 and not state.get("me",{}).get("ready",false))
 	if is_instance_valid(timer_label):
 		var time_text="%02d 秒"%maxi(0,int(deadline-now()-own_penalty))
@@ -1240,7 +1257,7 @@ func _process(delta):
 		var left=maxf(0,deadline-now()-own_penalty)
 		if trim.recruiting and left<20 and absf(left-trim.remaining)>=.05:trim.remaining=left;trim.queue_redraw()
 	if not online:return
-	if multiplayer.is_server() and game.phase=="recruit":
+	if (multiplayer.is_server() and not (cloud and cloud.active)) and game.phase=="recruit":
 		var expired=false
 		for p in game.players:expired=game.TOMORROW.expire(game,p,deadline-now()) or expired
 		if expired:broadcast()
@@ -1256,7 +1273,7 @@ func _process(delta):
 			game.begin_settling()
 			deadline=now()+game.settling_seconds
 			broadcast()
-	elif multiplayer.is_server() and game.phase=="settling" and now()>=deadline:
+	elif (multiplayer.is_server() and not (cloud and cloud.active)) and game.phase=="settling" and now()>=deadline:
 		game.battle()
 		var longest=0.0
 		for r in game.replays.values():
@@ -1265,7 +1282,7 @@ func _process(delta):
 			longest=maxf(longest,duration)
 		deadline=now()+maxf(longest+2,5)
 		broadcast()
-	elif multiplayer.is_server() and game.phase=="combat" and now()>=deadline:
+	elif (multiplayer.is_server() and not (cloud and cloud.active)) and game.phase=="combat" and now()>=deadline:
 		game.finish_combat()
 		deadline=now()+recruit_seconds()
 		broadcast()
@@ -1279,6 +1296,8 @@ func _process(delta):
 	if state.get("phase","")=="combat" and settlement_deadline>0 and now()>=settlement_deadline:
 		settlement_deadline=0;settlement_complete=true
 		if not gallery:render()
+	if cloud and cloud.active and state.get("phase","")=="combat":
+		if settlement_complete or state.get("replay",{}).get("events",[]).is_empty():cloud.finish_replay()
 	if not test_mode.is_empty():test_tick(delta)
 
 func test_check(ok:bool,message:String)->bool:
