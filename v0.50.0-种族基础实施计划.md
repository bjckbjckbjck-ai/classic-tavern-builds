# 七族共同判定与界面信息实施计划

> **For agentic workers:** Use superpowers:subagent-driven-development for bounded migrations, with specification and quality reviews. 本计划是完整 [v0.50.0 卡池设计](v0.50.0-七族卡池设计.md) 的规则基础阶段；不代表新卡、法术或发布已完成。

**Goal:** 让双种族和全部种族在现有招募、战斗、饰品、AI、磁力及图鉴中按同一规则生效，并让玩家看清种族和星级。

**Architecture:** 新建纯函数 `scripts/tribes.gd`，保留旧单族数据；各系统只迁移成员资格和每类一个的目标分配，实体、共享池、计时和回放结构不变。多族增益使用最大匹配，单个 UID 一次最多获得一份。新候选卡随后按完整设计接入。

**Tech Stack:** Godot 4.6.1、GDScript、现有原生截图与 headless 功能测试。

## 1. 种族模块与数据校验

文件：新增 `scripts/tribes.gd`、`tests/tribes_v50_test.gd`；修改 `scripts/catalog.gd`。

- [x] 添加成员规范化、归属、交集、显示、效果筛选和每类一个不同随从的最大匹配。
- [x] 以实际字典对象返回目标，不复制对象；同 UID 去重，权威 RNG 打乱等价候选。
- [x] 卡库拒绝未知族、重复族、空多族集合和错误布尔值。法术不作为种族随从。
- [x] 验证以下行为及 128 个随机小集合与穷举最优匹配一致：

```gdscript
var all = {"uid":"all", "all_tribes":true}
check(T.members(all).size()==7, "all means current seven tribes")
check(T.select_distinct([all]).size()==1, "one entity gets one buff")
check(T.select_distinct([all,{"uid":"fish","tribe":"鱼人"}]).size()==2, "flexible target does not steal the fish slot")
check(not T.belongs({"kind":"spell","all_tribes":true},"龙"), "spells are not minions")
```

运行：`& 'D:/godot/Godot_v4.6.1-stable_win64.exe/Godot_v4.6.1-stable_win64_console.exe' --headless --path . --script res://tests/tribes_v50_test.gd`，要求退出 0 且无脚本错误。

## 2. 全部规则调用点迁移

文件：`scripts/rules.gd`、`recruit_effects.gd`、`economy.gd`、`tavern_spells.gd`、`combat.gd`、`expansion_combat.gd`、`undead.gd`、`undead_combat.gd`、`issue_combat.gd`、`summon_effects.gd`、`trinkets.gd`、`trinket_policy.gd`、`trinket_recommend.gd`、`bot_ai.gd`、`boss_plan.gd`。

- [x] 成员判定统一为 `T.belongs(unit, tribe)`；可选种族效果用 `T.matches(unit, effect)`；自然祝福等共享种族效果用 `T.shares(unit, recipient)`。
- [x] 光牙、自然之力、蜡烛女王等每类一个使用 `T.select_distinct(board, rng)`；旧效果若有个数上限继续保留。
- [x] 共享池只通过原始卡 ID 抽一次；不遍历目标每个种族重复抽牌。
- [x] AI 的混合阵容收益使用 `T.distinct_count(board)`，普通种族需求按成员计数，不能把一个全部种族判为七个目标。
- [x] 添加真实操作链：融合怪磁力→毒鳍选中→亡灵永久攻击→每类一个；自然祝福并集每目标只一次；饰品门槛一个融合怪仍仅贡献一个。
- [x] 执行现有规则、叠层、亡灵、招募、法术、饰品、AI 和池完整性套件；不得用胜率模拟替代功能验证。

迁移形式：

```gdscript
const T=preload("res://scripts/tribes.gd")
var options=board.filter(func(u):return T.belongs(u,"机械"))
for target in T.select_distinct(board,rng):
	buff(target,effect,multiplier)
```

## 3. UI 信息统一与实机截图

文件：`scripts/main.gd`、`card.gd`、`presentation_fx.gd`，实际图鉴模块以当前 `rg` 结果定位。

- [x] 详情、卡面、图鉴过滤、战吼与磁力预览均读取同一模块；全部种族显示“全部”，详情列出七族。
- [x] 图鉴“所有卡”与“全部种族随从”分开筛选；双族按任一成员都能找到。
- [x] 非椭圆随从卡面加入独立星级旗标，避免金色和原创标记覆盖；不增加静态文字密度。
- [x] 用用户实际桌面图一／手机图二、三与本次原生运行截图对照，保留桌面底部微扇形、手机首次点击仅展开。
- [x] 检查 4/10 手牌、详情、图鉴和 2D 回退；记录手机布局模拟与 Android 真机的区别。

## 4. 评审、记录、后续

- [x] 先核对本计划要求，再审查代码质量；解决实际发现的问题。
- [x] 保存原生日志、截图与功能结果；更新下一阶段计划及 issue 进展，明确未发布。
- [x] 源码按具体文件提交，不夹带旧的未跟踪文件；公开仓库仅同步已约定的文档与证据。

2026-09-16 基础阶段记录：源码提交 `5859829`，公开文档与证据提交 `2d0be59`；28 套功能回归共 919 条、原生界面检查 32 条通过，保留九张截图。已回复并回读确认 [#25 进展](https://github.com/bjckbjckbjck-ai/classic-tavern-builds/issues/25#issuecomment-5692603486) 和 [#18 进展](https://github.com/bjckbjckbjck-ai/classic-tavern-builds/issues/18#issuecomment-5692603626)，两条保持开放。手机截图为 Windows 平台布局模拟；未发布本阶段 APK／EXE。用户所说“平铺”始终指微扇形平铺，已写入 `AGENTS.md`。

下一阶段继续完整 v0.50.0 设计中的 12 随从、4 法术、资源演出、卡框、手机关键血甲可见性，以及 APK／EXE／双客户端验收。基础阶段不提前更改发布版本或关闭 #25。
