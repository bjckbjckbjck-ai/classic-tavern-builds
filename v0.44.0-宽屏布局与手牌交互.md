# v0.44.0 · 宽屏布局与手牌交互

接续 [issue #18](https://github.com/bjckbjckbjck-ai/classic-tavern-builds/issues/18) 和 [v0.43.0审查](v0.43.0-UI体验审查.md)。本版优先处理屏幕空间、手牌交互和阶段演出，未调整卡池或战斗平衡。

## 完成项

| 上版待办 | 本版变化 | 验收重点 |
|---|---|---|
| 16:9 / 20:9仍留宽黑边 | 横屏画布按实际比例扩展，侧栏、场地、菜单分别布局；人物卡图保持比例 | 16:10、16:9、20:9实际窗口；2D命中区与3D相机投影一致 |
| 随从偏瘦、场地密度不合适 | 场上随从采用更宽的比例，整排按可用宽度排列；七随从及额外酒馆法术一起适配 | 满场七人、商店和法术均在边界内；站位预览使用同一计算 |
| 手牌像第三排资料卡 | 右下扇形收纳，悬停或选中时抬起并展开，邻牌让位；技能与计数移到英雄左侧 | 三种比例下逐张点击10张手牌，拖中间牌上阵，购买第10张牌 |
| 宽屏可能让购买/箭头失准 | 购买范围、战吼预览、插入线、招募效果、三连路径与英雄结算共享布局坐标 | 扩展区域边缘购买一次扣币；技能实际命中指定随从；回退2D仍可见 |
| 对手英雄像小资料条 | 对手使用与鲍勃/己方统一的拱形头像，护甲、生命与技能分开安放 | 招募到战斗转换、头像位置、实际结算与淘汰显示 |
| 战斗文字抢视觉中心 | 过程信息收至场地下沿的小字记录，中心留给攻击与效果 | 保留事件描述，不删规则事件；伤害数字仍出现在目标附近 |
| 刷新/升级反馈相似 | 刷新逐张展开，升级亮起酒馆等级并短暂显示提示；计时与操作持续 | 刷新和升级只扣费一次，演出不发送额外操作 |
| 金色缺少局部光泽 | 金色入场/聚焦时沿边缘扫过一道暖光；一次约0.9秒，结束后隐藏 | 不持续重绘卡牌纹理；减弱动态关闭扫光 |

## 过程中修复的问题

- 扇形手牌聚焦后，旧牌虽然放回原位，却还处在输入顺序最上层，会挡住邻牌。现在聚焦离开时恢复整个手牌输入顺序；聚焦只在手牌组内抬高，奖励窗口保持在上方。
- 屏幕尺寸变化时，3D英雄徽座曾保留旧位置，导致头像和生命数字分离。现在徽座每帧跟随真实头像控件；不同屏幕比例有单独的投影检查。
- 直接把桌面拉宽会增加离屏渲染像素。低、中画质按原有像素预算调整视口尺寸，保留宽屏视野；文字和交互画布不跟随降分辨率。高画质维持完整尺寸。
- 打开设置等页面后调整窗口尺寸，保留现有控件和值，不以重建页面来重置输入。
- 转盘内容居中时，遮罩同时覆盖宽屏两侧的输入区域，防止点击穿过后误触结束回合。

## 验证与边界

- 相关回归157条不同检查：布局68、设置9、既有审查21、3D体验22、工具栏4、施法交互8、磁力/三连及拖拽立体感25。重复运行不重复计数。使用实际输入事件验证操作，另在原生窗口截图检查。
- 补跑默认78套功能回归，最终日志共1508条断言通过，包含上述157条。首轮暴露了旧坐标、旧设置单页、旧排名/收入文字等过时检查；按当前控件及实际值维护后定向复跑通过。新布局和上版审查套件已进入默认构建列表。
- Android和Windows最终安装包验证、截图及文件校验以 [本版验证记录](validation/v0.44.0/VERIFICATION.md) 为准。原生截图使用确定的局面与76秒展示值；不是自然打完整场次。
- 本版只针对横屏，未承诺竖屏操作布局。手机触控以模拟器记录为边界；实体手机长局发热、帧间隔、极端卡牌效果数量仍需实测。
- 限制像素预算属于资源控制，不能据此宣称所有手机帧率提高。金色扫光也没有替换原有三连规则或额外增加收益。

## 下一步

1. **页面材质与层次**：主菜单用木牌导航，房间用座位卡，图鉴用册页，减少重复的大金框。遵守AGENTS：每批4–6格手绘图集，保留映射、提示词及裁剪记录。
2. **手机阅读**：10手牌状态下更明确的选中卡详情入口，验证长按、手指遮挡和单手操作；按机型反馈调整展开幅度。
3. **奖励节奏**：在已有三连汇聚和本版金色扫光基础上补奖励停顿与不同价值的声音强度；检查减弱动态及多人快照到达时的连续性。
4. **音乐与真机**：继续#17的合适音乐资源，单独验证混音与前后台切换；收集实体安卓手机长局性能，不用模拟器代替真机结论。

本版推进了#18的首要布局与演出待办。菜单/房间/图鉴的整体改造、音乐和真机长局验收继续跟踪，issue保持开放。
