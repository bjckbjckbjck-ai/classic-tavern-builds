# v0.24.0 · 饰品体验与交互修复

依据 [issue #5：饰品版本建议](https://github.com/bjckbjckbjck-ai/classic-tavern-builds/issues/5) 的五项反馈。保持七族、叠层机制、130张在池随从、34张酒馆法术、16张暗月奖品、32件饰品与20位英雄；这一版聚焦体验和规则修复，没有进行平衡胜率模拟。

| 反馈 | 原因与本版行为 | 扩大检查范围 |
|---|---|---|
| 电脑端感觉卡顿 | 战斗每帧原先重建整套控件；改为保留底部英雄、计时器、侧栏等静态区域，仅更新战斗帧。静态棋盘不再每帧重画；纹理有192项有界缓存。 | 重复招募快照不打断拖动；招募同步不再携带上一场战斗回放；相同手牌状态按索引复用。设置提供60/120 FPS目标上限，默认60。 |
| 转盘结果不直观 | 停转后展示结果1秒，自动回到酒馆。再按实际结算顺序显示法术卡、目标增益、吞食及奖品逐张飞入手牌。 | 可略过特效；多次转盘排队；手动打开历史不强制关闭；重复同步不再播放或领奖，阶段切换取消旧特效。 |
| 买卖区域被卡挡住 | 卡面与空白落点统一解释：商店牌落到手牌或战场即购买，战场牌落到酒馆随从/法术/空白即出售。手牌背景接收区域扩大，密集手牌扣除卡间距后布局。 | 9张手牌买第10张；满手/缺币不显示有效购买；过期拖动UID拒绝；卡牌触控与鼠标共用处理。 |
| 战斗中缺少信息 | 上方增加对手英雄头像、护甲、星级、技能、饰品图标；技能/饰品可点按查看公开说明。底部缩小展示自己的手牌，上方显示对手牌背和数量。 | 明牌随回放更新属性，低音提琴已召唤的手牌灰显，长按查看；对手手牌身份在发送前删除；阵亡后仍能回看自己本场手牌。 |
| 买到金色上场没有奖励 | 金色随从进入手牌时，尚未领奖则标记待领取；首次上阵或磁力合体获得三连奖励，按打出时酒馆等级+1、最高6星。 | 点金购买、尤格魔盒、偷取金色、暗月提高身价、三连合成共用入口；已领奖实例回手不重复，B-Box磁力复制不复制领奖资格。 |

## 对v0.23规则的更正

上一版把“点金不是三连合成”错误地扩大成了“不产生打出奖励”。现在**在场变金本身不立即发奖，但金色随从进入手牌后首次打出可发奖**。`reward_claimed` 跟随实例，`reward` 表示待领取；不按物理池副本数决定能否领取。点金随从仍保留原来的真实副本数，出售不会凭空归还三份。

奖励依然是一张需要使用的“三连奖励”法术。上场战吼占满手牌时，奖励遵循现有10张手牌上限；图鉴与详情已更新。已领奖的金色再次回手不会产生第二份奖励，不追溯给旧版已经打出的牌补发。

## 转盘的展示与结算

房主一次完成规则结算，并发送有序 `steps`（来源卡、入手卡、目标属性变化）。客户端只播放这些数据，不执行任何规则或随机取样。轮盘、停留及随后特效均不暂停回合；到点仍会开战。玩家可以直接略过，奖励不会消失。

施法结果按每张法术分别记录，奖品使用真正生成的卡牌图像；不是把一次总属性变化猜测成动画。当前展示目标属性和金色变化，个别没有属性变化的持续效果以法术卡与名称提示。轮盘规则和最近记录仍可手动查看；12条完整历史选择器留待后续，不在本版宣称完成。

## 性能证据与边界

本机固定场景30次界面更新的headless测量：旧实现中位7.791ms、P95 8.142ms；首次改造后中位2.141ms、P95 2.345ms；最终版本中位2.505ms、P95 2.982ms。新实现额外展示了缩小手牌和对手图标。此项测的是GDScript界面构建开销，不包含完整GPU显示耗时，也不能推断所有电脑或手机的实际帧率。

默认60 FPS上限，体验设置可切换120；这是上限，仍受显示器、垂直同步、硬件和复杂战斗逻辑限制。复制体验诊断可提供平均/P95帧间隔。复杂长亡语链仍然同步计算，未在这一版改为后台线程。

## 开发约束与验证入口

- `scripts/main.gd`：`drop_destination`统一区域；拖动携带UID；`combat_frame/refresh_combat`只重建动态战斗节点。
- `scripts/rules.gd`：`add_hand`设置金色奖励，`reward_claimed`阻止重复；`private_replay`输出本席位手牌状态与敌方数量；`replay_hand`读取帧索引。
- `scripts/darkmoon.gd`：仅规则端记录 `steps`；`darkmoon_ui.gd`负责转轮、停留、队列；`wheel_feedback.gd`负责顺序特效。
- `scripts/combat_hud.gd`：公开对手信息、私有本方手牌和敌方牌背。禁止将原始 `combat.events[].hands` 直接通过RPC广播。
- 回归：`python scripts/verify.py`；双进程专用验证 `tests/issue_network_v24.gd`；实际拖动与转盘时序 `tests/issue_ui_v24_test.gd`；性能固定场景 `tests/perf_v24.gd`。安装包、签名及实机范围见 `VERIFICATION.md`。

参考：[Godot自定义2D绘制文档](https://docs.godotengine.org/en/stable/tutorials/2d/custom_drawing_in_2d.html)说明绘制命令可缓存、状态变化时再请求重绘。本版性能修改以本机代码和测量为依据，没有把卡顿直接归因为帧率设置。

## 下一步建议

1. 优先收集实体安卓和出现卡顿电脑的同阵容诊断，区分界面帧间隔、招募结束逻辑停顿和网络等待。
2. 改进长亡语链的分段计算与等待提示；考虑独立战斗工作线程前先隔离随机数与数据写回。
3. 增加可点选的12条转盘历史，以及持续效果、全局成长的更明确剩余次数提示。
4. 扩大手牌鱼、金色磁力和多个饰品交互的真人对局反馈，再决定价格与卡池轮换；暂不新增种族。
