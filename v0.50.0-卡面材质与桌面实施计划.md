# v0.50.0 卡面材质与桌面实施计划

> 执行既有 #18 与《下一阶段开发计划》的卡框/桌面步骤；使用 godot、imagegen、writing-plans 与 subagent-driven-development，按规格和质量顺序复查。既有双端微扇形与七族规则保持不变。

目标：手牌、发现、图鉴和详情呈现有厚度的拱顶卡片、拱形画窗、弯曲皮革名条与浅色纸页；在小尺寸仍能看清主体、层级、星级与费用。桌面降低密纹理与金边干扰，实测操作和返回性能。

架构：共享 `card_surface.gd` 提供轮廓/画窗/材质/文字区域；现有 `card.gd` 与 3D painter 共用绘制，`card_actor.gd` 体积轮廓对应，2D 输入和微扇形布局不另复制。新增一张2×2材质图集，裁剪成纸页、皮革、哑金和桌面木材；图像不生成文字、数值、卡框或角色，以代码确保边界稳定。背景2D/3D共用配色与材质处理。保留原始图集、坐标、提示词和哈希。

## 具体工作

- [x] 重新读取开放 issue 与最新回复；本轮只发现 #18/#25，上一轮源码/证据提交属于有效进展。
- [x] 固定对照：用户真实图一电脑底部中央、图二/三手机右下收纳及展开；在实际项目中截图记录前后差别。生成四格无字材质图集，检查整图和每格后导入。
- [x] `scripts/card_surface.gd` 与 `scripts/card.gd`：共享拱顶卡片轮廓和内画窗，厚外缘/暗内缘/方向高光，弧形名条；纸页等比纹理，去重描边，普通/金色区分。短名字居中、长名字适配，长效果省略后可展开详情。星级、原创标记、金色标记、攻血/族标签和法术费用不相互遮挡。
- [x] `scripts/presentation3d/card_actor.gd`：非椭圆卡片的厚度网格跟随新轮廓，不能透明拱顶下面仍露出矩形硬块；辉光和接触阴影匹配新轮廓；不按帧重新创建网格或载图。
- [x] `scripts/main.gd`、桌面 shader：2D与3D使用统一桌面材质/色调，简化中央细纹，压低边框亮度，保证3D回退与宽屏一致；不加入每帧CPU图像处理。
- [x] `tests/card_surface_v50_test.gd`：原生绘制和有限输入验收，普通/金色/法术/原创/长名/长文、多族、10手牌、桌面/手机4牌与10牌、收纳展开、2D/3D、发现、图鉴、详情、16:9/20:9；购买/出牌/重排/取消目标可用。
- [x] 性能记录：同一场景与画质下，记录静置帧间隔与收藏往返耗时；确认卡面静置不重绘、反复返回不会无界增加节点/纹理。先测再决定缓存修改；不声称Windows结果代表真机性能。
- [x] 独立规格后质量审查；修复实际发现的问题，再做相关回归，不做胜率模拟。更新公告、证据、差距表与下一阶段文档，显式提交本地源码，公开仓同步文档/证据，回复 issue。

## 验收与未完成范围

优先采用共享几何加手绘材质：纯色描边无法改变质感，整张预烘焙卡片则难以适配长中文、动态费用和叠层；本方案让美术和数据各自可扩展。桌面只改视觉数据，不改变联机时钟或规则。

完整 v0.50 发行仍要处理全局卡池生成器旧口径、正式版本/公告、APK/EXE及Android与安装包联机验证。#25待完整交付关闭；#18的性能、音频等剩余范围继续逐项核对，不因本轮局部视觉改善提前关闭。

2026-09-17执行结果：共享卡面/厚度与桌面已接入；独立规格、质量审查通过。9项相关回归（8项原生271条与几何检查）通过。具体截图、性能数据与尚未发布范围见 `validation/issue18-card-surfaces/README.md`。
